---
permalink: "characters/relationships/data.js"
---
const data = {
    ch: [
        {% for ch in characters %}
            {
                id: "{{ ch.name }}",
                color: "{{ ch.color }}"
            },
        {% endfor %}
    ],
    rel: [
        {% for rel in relationships %}
            {
                source: "{{ rel.ch[0][0] }}",
                target: "{{ rel.ch[1][0] }}",
                rel1: "{{ rel.ch[0][1] }}",
                rel2: "{{ rel.ch[1][1] }}"
            },
        {% endfor %}
    ]
};
{% comment %} const data = {
    ch: [
            {
                name: "Sparky",
                color: "#00ccff"
            },
            {
            name: "Peacock",color: "#20f"
            }
    ],
    rel: [
        {
            ch1: "Sparky",ch2: "Peacock",rel1:"Stepdaughter",rel2:"Stepfather"
        }
    ]
}; {% endcomment %}