<ul>
    {% for story in stories %}
        {% assign desc = story.content | wordCount | append: " words — " | append: story.data.desc %}
        {% include 'fileLi', file: story, desc: desc %}
    {% endfor %}
</ul>
