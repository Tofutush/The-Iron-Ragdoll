---
layout: chsLayout.liquid
pagination:
  data: "{{ collection.stories | getStoryTags }}"
  size: 1
  alias: tag
  addAllPagesToCollections: true
permalink: "stories/tag/{{ tag | slug }}/"
eleventyComputed:
  title: "{{ tag }} Stories"
  tag: "{{ tag }}"
  eleventyNavigation:
    key: "{{ tag }} Stories"
    title: "{{ tag }}"
    parent: Story Tags
---
{% assign filteredStories = collection.stories | filterStoriesByTag: tag %}
{% include 'storylist', chs: filteredStories %}