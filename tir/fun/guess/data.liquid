---
permalink: "fun/guess/data.js"
---
{% assign data = characters | getGuessData: relationships %}
const data = {{ data | json }};

const imgs = {
    {% for ch in data.ch %}{% assign profile = ch | lowerCase | append: ' profile' %}{% assign thumb = ch | lowerCase | append: ' thumb' %}{% capture url %}{% imageUrl 'gallery/' profile 'png' 200 thumb 'png' %}{% endcapture %}{{ ch }}: "{{ url | htmlBaseUrl }}",{% endfor %}
};
