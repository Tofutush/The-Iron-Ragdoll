---
permalink: "fun/guess/data.js"
---

const imgs = {
    {% for ch in characters %}{% assign profile = ch.name | lowerCase | append: ' profile.png' %}{% assign thumb = ch.name | lowerCase | append: ' thumb.png' %}{% capture url %}{% imageUrl 'gallery/' profile 200 thumb %}{% endcapture %}{{ ch.name }}: "{{ url | htmlBaseUrl }}",{% endfor %}
};

const ch = [{% for ch in characters %}"{{ ch.name }}",{% endfor %}];

const rel = {{ relationships | json }}
