---
layout: layout.liquid
title: Random World File
noNav: true
---
<p>Takes you to a random world file! You need to enable JavaScript for this.</p>
<script>
	const files = [
		{% for file in collections.all %}
			{% if file.url contains "/world/" %}
				"{{ file.url | htmlBaseUrl }}",
			{% endif %}
		{% endfor %}
	];
	window.location.href = `${window.location.protocol}//${window.location.host}${files[Math.floor(Math.random() * files.length)]}`;
</script>