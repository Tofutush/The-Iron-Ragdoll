---
permalink: "characters/relationships/data.js"
---
const data = {
ch: [
{% for ch in characters %}
{
id: "{{ ch.name }}",
color: "{{ ch.color }}",
img: "{% assign name = ch.name | lowerCase | append: ' thumb' %}{% capture url %}{% imageUrl name 200 %}{% endcapture %}{{ url | htmlBaseUrl }}",
},
{% endfor %}
],
rel: [
{% for rel in relationships %}
{
source: "{{ rel.ch[0][0] }}",
target: "{{ rel.ch[1][0] }}",
rel1: "{{ rel.ch[0][1] }}",
rel2: "{{ rel.ch[1][1] }}",
{% if rel.desc == '' %}minor: true{% endif %}
},
{% endfor %}
]
};
